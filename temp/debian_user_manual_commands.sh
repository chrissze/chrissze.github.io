#/bin/bash

# THIS SCRIPT MUST BE RUN BY A NON-ROOT USER BY MANUAL COPY AND PASTE.

# If I use script to run, this script must be run in a terminal with GUI, or simply locally.
gsettings set org.gnome.shell.app-switcher current-workspace-only true



